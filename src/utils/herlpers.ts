import {
  responsiveHeight,
  responsiveWidth,
  responsiveFontSize,
} from 'react-native-responsive-dimensions';

export const svh = responsiveHeight;
export const svw = responsiveWidth;
export const sfv = responsiveFontSize;
